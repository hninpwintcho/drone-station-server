# Drone Station Server — Environment Variables

# ── MQTT Broker (internal Docker network) ──────────
MQTT_HOST=mqtt
MQTT_PORT=1883

# ── MediaMTX RTSP/WebRTC Server (internal) ─────────
RTSP_HOST=rtsp
RTSP_API=http://rtsp:9997

# ── EC2 Public IP (for WebRTC ICE) ─────────────────
# ⚠️ Also update mediamtx/mediamtx.yml:
#    webrtcICEUDPMuxAddress: YOUR_EC2_PUBLIC_IP:8890
EC2_PUBLIC_IP=15.164.50.229
